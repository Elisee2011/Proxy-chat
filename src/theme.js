const savedTheme=localStorage.getItem('proxy-chat-theme');
if(savedTheme==='dark')document.documentElement.classList.add('dark-theme');
const addThemeButton=()=>{const topbar=document.querySelector('.topbar');if(!topbar||document.querySelector('#themeBtn'))return;const btn=document.createElement('button');btn.id='themeBtn';btn.className='theme-toggle';btn.type='button';btn.title='Changer de thème';const update=()=>{const dark=document.documentElement.classList.contains('dark-theme');btn.textContent=dark?'☀️ Mode clair':'🌙 Mode sombre';btn.setAttribute('aria-label',dark?'Activer le mode clair':'Activer le mode sombre');};btn.addEventListener('click',()=>{const dark=document.documentElement.classList.toggle('dark-theme');localStorage.setItem('proxy-chat-theme',dark?'dark':'light');update();});topbar.insertBefore(btn,topbar.querySelector('#profileBtn'));update();};
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',addThemeButton);else addThemeButton();
