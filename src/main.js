import './style.css';

const SERVER_URL = import.meta.env.VITE_SERVER_URL || (location.protocol === 'https:' ? `wss://${location.host}` : `ws://${location.hostname}:3001`);
const WORLD={width:2400,height:1500},CHAT_RADIUS=360,VOICE_RADIUS=260;
const ICE_SERVERS=[{urls:'stun:stun.l.google.com:19302'},{urls:'stun:stun1.l.google.com:19302'}];
const savedName=localStorage.getItem('proxy-chat-name');
const state={socket:null,connected:false,players:new Map(),messages:[],pendingChats:[],keys:new Set(),micStream:null,peers:new Map(),audio:new Map(),voiceRequested:new Set(),id:crypto.randomUUID(),name:savedName||`Player${Math.floor(100+Math.random()*900)}`,x:1200,y:750,speed:5,joystick:{active:false,x:0,y:0}};
const app=document.querySelector('#root');
app.innerHTML=`<div class="app"><header class="topbar"><div class="brand"><span class="brand-dot"></span><strong>Proxy</strong><span>Chat</span></div><div class="status"><i id="statusDot"></i><span id="statusText">Connexion…</span></div><button class="ghost" id="profileBtn">👤 <span id="profileName"></span></button></header><main class="game-shell"><section class="world-wrap"><canvas id="world" width="1200" height="760"></canvas><div class="hint">🕹️ Joystick ou <b>ZQSD</b> pour bouger · <b>Entrée</b> pour envoyer</div><div class="voice-pill" id="voicePill">🎙️ Micro désactivé</div><div class="coords" id="coords">X: 1200 · Y: 750</div><div class="joystick" id="joystick"><div class="joystick-knob" id="joystickKnob"></div></div></section><aside class="chat-panel"><div class="chat-head"><div><h1>Chat de proximité</h1><p id="nearby">Recherche des joueurs…</p></div><span class="live">LIVE</span></div><div class="messages" id="messages"></div><form class="composer" id="composer"><input id="messageInput" maxlength="180" autocomplete="off" placeholder="Écris aux personnes proches…"/><button type="submit">➤</button></form><div class="controls"><button class="control" id="micBtn">🎙️ Activer le micro</button><button class="control" id="centerBtn">⌖ Recentrer</button></div><div class="teleport"><div class="section-title">📍 Téléportation</div><div class="my-coords">Tes coordonnées : <b id="myCoords">X: 1200 · Y: 750</b></div><div class="tp-inputs"><input id="tpX" type="number" min="0" max="2400" placeholder="X"/><input id="tpY" type="number" min="0" max="1500" placeholder="Y"/><button id="tpBtn">Aller</button></div><div class="section-title players-title">👥 Joueurs</div><div id="playerList" class="player-list"><span class="empty">Aucun autre joueur</span></div></div></aside></main><div class="modal hidden" id="profileModal"><div class="modal-card"><button class="close" id="closeModal">×</button><h2>Ton profil</h2><label>Pseudo<input id="nameInput" maxlength="18"/></label><button class="primary" id="saveName">Enregistrer</button></div></div></div>`;
const canvas=document.querySelector('#world'),ctx=canvas.getContext('2d'),messagesEl=document.querySelector('#messages'),nearbyEl=document.querySelector('#nearby'),statusDot=document.querySelector('#statusDot'),statusText=document.querySelector('#statusText'),input=document.querySelector('#messageInput'),micBtn=document.querySelector('#micBtn'),voicePill=document.querySelector('#voicePill'),coordsEl=document.querySelector('#coords'),myCoordsEl=document.querySelector('#myCoords'),playerListEl=document.querySelector('#playerList'),joystick=document.querySelector('#joystick'),joystickKnob=document.querySelector('#joystickKnob');
const escapeHtml=v=>String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const distance=(a,b)=>Math.hypot(a.x-b.x,a.y-b.y);
function setStatus(ok,text){state.connected=ok;statusDot.className=ok?'ok':'';statusText.textContent=text;}
function addMessage(m){state.messages.push(m);state.messages=state.messages.slice(-60);messagesEl.innerHTML=state.messages.map(x=>`<div class="msg ${x.player?.id===state.id?'mine':''}"><div><b>${escapeHtml(x.name)}</b><span>${new Date(x.at||Date.now()).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})}</span></div><p>${escapeHtml(x.text)}</p></div>`).join('');messagesEl.scrollTop=messagesEl.scrollHeight;}
function sendChat(){const text=input.value.trim();if(!text)return;const m={type:'chat',text};addMessage({name:state.name,text,at:Date.now(),player:{id:state.id,x:state.x,y:state.y}});if(state.socket?.readyState===WebSocket.OPEN)state.socket.send(JSON.stringify(m));else state.pendingChats.push(m);input.value='';input.focus();}
document.querySelector('#composer').addEventListener('submit',e=>{e.preventDefault();sendChat();});
function flushPendingChats(){if(state.socket?.readyState!==WebSocket.OPEN)return;for(const m of state.pendingChats)state.socket.send(JSON.stringify(m));state.pendingChats=[];}
function broadcastPosition(){if(state.socket?.readyState===WebSocket.OPEN)state.socket.send(JSON.stringify({type:'move',x:state.x,y:state.y}));}
function updateCoords(){const t=`X: ${Math.round(state.x)} · Y: ${Math.round(state.y)}`;coordsEl.textContent=t;myCoordsEl.textContent=t;}
function renderPlayerList(){const list=[...state.players.values()];if(!list.length){playerListEl.innerHTML='<span class="empty">Aucun autre joueur</span>';return;}playerListEl.innerHTML=list.map(p=>`<div class="player-row"><div><b>${escapeHtml(p.name)}</b><small>X: ${Math.round(p.x)} · Y: ${Math.round(p.y)}</small></div><button data-teleport="${p.id}">📍 Aller</button></div>`).join('');playerListEl.querySelectorAll('[data-teleport]').forEach(b=>b.addEventListener('click',()=>{const p=state.players.get(b.dataset.teleport);if(p)teleportTo(p.x,p.y);}));}
function teleportTo(x,y){state.x=Math.max(0,Math.min(WORLD.width,Number(x)||0));state.y=Math.max(0,Math.min(WORLD.height,Number(y)||0));broadcastPosition();updateCoords();}
function sendVoice(type,to,extra={}){if(state.socket?.readyState===WebSocket.OPEN)state.socket.send(JSON.stringify({type,to,...extra}));}
function connect(){try{state.socket=new WebSocket(SERVER_URL);}catch{setStatus(false,'Hors ligne');return;}state.socket.onopen=()=>{setStatus(true,'En ligne');state.socket.send(JSON.stringify({type:'join',id:state.id,name:state.name,x:state.x,y:state.y}));flushPendingChats();};state.socket.onclose=()=>{setStatus(false,'Serveur indisponible');for(const id of [...state.peers.keys()])closePeer(id);setTimeout(connect,2500);};state.socket.onerror=()=>setStatus(false,'Connexion interrompue');state.socket.onmessage=async e=>{let d;try{d=JSON.parse(e.data)}catch{return;}if(d.type==='snapshot'){state.players.clear();for(const p of d.players)if(p.id!==state.id)state.players.set(p.id,p);updateNearby();renderPlayerList();}else if(d.type==='player:join'&&d.player.id!==state.id){state.players.set(d.player.id,d.player);renderPlayerList();if(state.micStream&&distance(state,d.player)<=VOICE_RADIUS)requestVoice(d.player.id);}else if(d.type==='player:move'&&d.player.id!==state.id){state.players.set(d.player.id,d.player);renderPlayerList();}else if(d.type==='player:name'&&d.player.id!==state.id){const p=state.players.get(d.player.id);if(p){p.name=d.player.name;renderPlayerList();}}else if(d.type==='player:leave'){state.players.delete(d.id);closePeer(d.id);state.voiceRequested.delete(d.id);renderPlayerList();}else if(d.type==='chat'){if(d.player?.id!==state.id&&d.player&&distance(state,d.player)<=CHAT_RADIUS)addMessage(d);}else if(d.type.startsWith('voice:'))await handleSignal(d);};}
function updateNearby(){const n=[...state.players.values()].filter(p=>distance(state,p)<=CHAT_RADIUS).length;nearbyEl.textContent=n?`${n} personne${n>1?'s':''} dans ta zone`:'Personne à proximité';}
window.addEventListener('keydown',e=>{if(e.key==='Enter'&&document.activeElement===input){e.preventDefault();sendChat();return;}if(['INPUT','TEXTAREA'].includes(document.activeElement?.tagName))return;state.keys.add(e.key.toLowerCase());});window.addEventListener('keyup',e=>state.keys.delete(e.key.toLowerCase()));
function setJoystick(e){const r=joystick.getBoundingClientRect(),cx=r.left+r.width/2,cy=r.top+r.height/2,max=r.width*.34;let dx=e.clientX-cx,dy=e.clientY-cy,len=Math.hypot(dx,dy);if(len>max){dx=dx/len*max;dy=dy/len*max;}state.joystick.x=dx/max;state.joystick.y=dy/max;joystickKnob.style.transform=`translate(${dx}px,${dy}px)`;}
joystick.addEventListener('pointerdown',e=>{e.preventDefault();state.joystick.active=true;joystick.setPointerCapture(e.pointerId);setJoystick(e);});joystick.addEventListener('pointermove',e=>{if(state.joystick.active){e.preventDefault();setJoystick(e);}});function releaseJoystick(){state.joystick.active=false;state.joystick.x=0;state.joystick.y=0;joystickKnob.style.transform='translate(0,0)';}joystick.addEventListener('pointerup',releaseJoystick);joystick.addEventListener('pointercancel',releaseJoystick);joystick.addEventListener('lostpointercapture',releaseJoystick);
function update(){let dx=0,dy=0;if(state.keys.has('z')||state.keys.has('arrowup'))dy--;if(state.keys.has('s')||state.keys.has('arrowdown'))dy++;if(state.keys.has('q')||state.keys.has('arrowleft'))dx--;if(state.keys.has('d')||state.keys.has('arrowright'))dx++;dx+=state.joystick.x;dy+=state.joystick.y;if(dx||dy){const len=Math.hypot(dx,dy),factor=Math.min(1,len);state.x=Math.max(40,Math.min(WORLD.width-40,state.x+(dx/len)*state.speed*factor));state.y=Math.max(40,Math.min(WORLD.height-40,state.y+(dy/len)*state.speed*factor));broadcastPosition();}updateNearby();updateVoiceDistances();updateCoords();}
function worldToScreen(x,y){const scale=Math.min(canvas.width/WORLD.width,canvas.height/WORLD.height)*.98;return{x:canvas.width/2+(x-state.x)*scale,y:canvas.height/2+(y-state.y)*scale,scale};}
function drawPlayer(p,me=false){const s=worldToScreen(p.x,p.y);if(s.x<-50||s.x>canvas.width+50||s.y<-50||s.y>canvas.height+50)return;ctx.beginPath();ctx.arc(s.x,s.y,16,0,Math.PI*2);ctx.fillStyle=me?'#8b5cf6':'#22d3ee';ctx.fill();ctx.lineWidth=3;ctx.strokeStyle='rgba(255,255,255,.75)';ctx.stroke();ctx.font='600 14px Inter,system-ui';ctx.textAlign='center';ctx.fillStyle='#fff';ctx.fillText(p.name,s.x,s.y-25);}
function render(){ctx.clearRect(0,0,canvas.width,canvas.height);const scale=Math.min(canvas.width/WORLD.width,canvas.height/WORLD.height)*.98;ctx.fillStyle='#D8C3A5';ctx.fillRect(0,0,canvas.width,canvas.height);const tile=64*scale;const ox=((WORLD.width/2-state.x)*scale)%tile;const oy=((WORLD.height/2-state.y)*scale)%tile;for(let row=-2;row<Math.ceil(canvas.height/tile)+2;row++){const y=row*tile+oy;const shift=(row&1)?tile/2:0;for(let col=-2;col<Math.ceil(canvas.width/tile)+2;col++){const x=col*tile+ox+shift;ctx.fillStyle='#D8C3A5';ctx.fillRect(x+1,y+1,tile-2,tile-2);ctx.strokeStyle='rgba(90,70,45,.38)';ctx.lineWidth=Math.max(1,3*scale);ctx.strokeRect(x+1,y+1,tile-2,tile-2);}}const zoneR=CHAT_RADIUS*scale;ctx.beginPath();ctx.arc(canvas.width/2,canvas.height/2,zoneR,0,Math.PI*2);ctx.fillStyle='rgba(139,92,246,.06)';ctx.fill();ctx.strokeStyle='rgba(139,92,246,.2)';ctx.lineWidth=1;ctx.stroke();for(const p of state.players.values())drawPlayer(p);drawPlayer({x:state.x,y:state.y,name:state.name},true);requestAnimationFrame(render);}
function createPeer(id){if(state.peers.has(id))return state.peers.get(id);const pc=new RTCPeerConnection({iceServers:ICE_SERVERS});pc.iceQueue=[];state.peers.set(id,pc);if(state.micStream)state.micStream.getTracks().forEach(t=>pc.addTrack(t,state.micStream));pc.onicecandidate=e=>{if(e.candidate)sendVoice('voice:ice',id,{candidate:e.candidate});};pc.ontrack=e=>{let a=state.audio.get(id);if(!a){a=document.createElement('audio');a.autoplay=true;a.playsInline=true;a.setAttribute('playsinline','');a.controls=false;a.style.display='none';document.body.appendChild(a);state.audio.set(id,a);}a.srcObject=e.streams[0]||new MediaStream([e.track]);a.play().catch(()=>{});};pc.oniceconnectionstatechange=()=>{if(pc.iceConnectionState==='failed')pc.restartIce();};pc.onconnectionstatechange=()=>{if(['failed','closed'].includes(pc.connectionState))closePeer(id);};return pc;}
function closePeer(id){const pc=state.peers.get(id);if(pc)pc.close();state.peers.delete(id);const a=state.audio.get(id);if(a){a.srcObject=null;a.remove();}state.audio.delete(id);}
async function makeOffer(id){const pc=createPeer(id);if(pc.signalingState!=='stable')return;try{const offer=await pc.createOffer({offerToReceiveAudio:true});await pc.setLocalDescription(offer);sendVoice('voice:offer',id,{offer:pc.localDescription});}catch(e){console.warn('voice offer',e);}}
async function handleSignal(d){if(d.from===state.id)return;try{if(d.type==='voice:request'){createPeer(d.from);if(state.micStream&&state.id<d.from)await makeOffer(d.from);else if(!state.micStream)sendVoice('voice:ready',d.from);return;}if(d.type==='voice:ready'){if(state.micStream&&distance(state,state.players.get(d.from)||{})<=VOICE_RADIUS)await makeOffer(d.from);return;}const pc=createPeer(d.from);if(d.type==='voice:offer'){await pc.setRemoteDescription(d.offer);for(const c of pc.iceQueue.splice(0))await pc.addIceCandidate(c);const answer=await pc.createAnswer();await pc.setLocalDescription(answer);sendVoice('voice:answer',d.from,{answer:pc.localDescription});}else if(d.type==='voice:answer'){await pc.setRemoteDescription(d.answer);for(const c of pc.iceQueue.splice(0))await pc.addIceCandidate(c);}else if(d.type==='voice:ice'&&d.candidate){if(pc.remoteDescription)await pc.addIceCandidate(d.candidate);else pc.iceQueue.push(d.candidate);}}catch(e){console.warn('voice signaling',e);}}
function requestVoice(id){if(!state.micStream||state.voiceRequested.has(id)||distance(state,state.players.get(id)||{})>VOICE_RADIUS)return;state.voiceRequested.add(id);createPeer(id);sendVoice('voice:request',id);}
function updateVoiceDistances(){for(const [id,a] of state.audio){const p=state.players.get(id);if(!p){closePeer(id);continue;}const d=distance(state,p);a.volume=d>VOICE_RADIUS?0:Math.max(.02,1-d/VOICE_RADIUS);}if(state.micStream)for(const p of state.players.values()){if(distance(state,p)<=VOICE_RADIUS)requestVoice(p.id);else{state.voiceRequested.delete(p.id);if(state.peers.has(p.id))closePeer(p.id);}}}
async function toggleMic(){if(state.micStream){state.micStream.getTracks().forEach(t=>t.stop());state.micStream=null;state.voiceRequested.clear();for(const id of [...state.peers.keys()])closePeer(id);micBtn.textContent='🎙️ Activer le micro';voicePill.textContent='🎙️ Micro désactivé';return;}try{if(!navigator.mediaDevices?.getUserMedia)throw new Error('Micro non disponible');state.micStream=await navigator.mediaDevices.getUserMedia({audio:{echoCancellation:true,noiseSuppression:true,autoGainControl:true,channelCount:1}});micBtn.textContent='🔴 Couper le micro';voicePill.textContent='🎙️ Micro actif · proximité';for(const p of state.players.values())if(distance(state,p)<=VOICE_RADIUS)requestVoice(p.id);}catch{voicePill.textContent='🎙️ Micro non autorisé';}}
micBtn.addEventListener('click',toggleMic);document.querySelector('#centerBtn').onclick=()=>teleportTo(WORLD.width/2,WORLD.height/2);document.querySelector('#tpBtn').onclick=()=>{const x=Number(document.querySelector('#tpX').value),y=Number(document.querySelector('#tpY').value);if(Number.isFinite(x)&&Number.isFinite(y))teleportTo(x,y);};document.querySelector('#profileBtn').onclick=()=>{document.querySelector('#nameInput').value=state.name;document.querySelector('#profileModal').classList.remove('hidden');};document.querySelector('#closeModal').onclick=()=>document.querySelector('#profileModal').classList.add('hidden');document.querySelector('#saveName').onclick=()=>{const n=document.querySelector('#nameInput').value.trim().replace(/[^\p{L}\p{N}_ -]/gu,'').slice(0,18);if(n){state.name=n;localStorage.setItem('proxy-chat-name',n);document.querySelector('#profileName').textContent=n;if(state.socket?.readyState===WebSocket.OPEN)state.socket.send(JSON.stringify({type:'rename',name:n}));}document.querySelector('#profileModal').classList.add('hidden');};document.querySelector('#profileName').textContent=state.name;updateCoords();setInterval(update,50);connect();render();